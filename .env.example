# Public site metadata (safe to expose in the browser bundle).
# Copy to `.env` for local dev; set the same names as Cloudflare Pages
# environment variables in production.

VITE_SITE_URL=https://nguyenlekhanh.pages.dev
VITE_CONTACT_EMAIL=nglekhanh2507@gmail.com
